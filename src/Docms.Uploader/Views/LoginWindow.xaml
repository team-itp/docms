<Window x:Class="Docms.Uploader.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Docms.Uploader.Views"
        xmlns:vm="clr-namespace:Docms.Uploader.Common"
        mc:Ignorable="d"
        Title="ログイン" Height="400" Width="400">
    <d:Window.DataContext>
        <vm:LoginViewModel />
    </d:Window.DataContext>
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="{Binding ErrorMessage}"/>
        <TextBox Grid.Row="1" TextWrapping="Wrap" Text="{Binding Username}" />
        <PasswordBox Grid.Row="2" PasswordChanged="PasswordBox_PasswordChanged"/>
        <Button Grid.Row="3" Content="ログイン" Command="{Binding LoginCommand}" />
    </Grid>
</Window>
