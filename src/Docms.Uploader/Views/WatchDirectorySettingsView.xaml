<UserControl x:Class="Docms.Uploader.Views.WatchDirectorySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Docms.Uploader.Views"
             xmlns:vm="clr-namespace:Docms.Uploader.ApplicationSettings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <d:UserControl.DataContext>
        <vm:WatchDirecotrySettingsViewModel/>
    </d:UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Grid.Row="1" Content="監視するフォルダーを指定してください。" />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="DirectoryToWatch" Text="{Binding WatchDirectory}" HorizontalAlignment="Stretch"/>
            <Button Grid.Column="1" Content="参照" Height="25" VerticalAlignment="Bottom" Click="Reference_Click"/>
        </Grid>

    </Grid>
</UserControl>
